# api-gateway-svc
API-Gateway service

1. `git checkout -b gzovdik/init-project-infra`
- сделать новую ветку
2. `git add . && git commit -m "коммент" && git push -u origin gzovdik/init-project-infra`
- добавить все файлы в коммит, сделать коммит и запушить его
